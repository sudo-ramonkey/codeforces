#include<iostream>
#include<vector>
using namespace std;

void solve()
{
    vector<int> v;
    int n;cin>>n;//length of sequence 
    int b = 0;
    while(cin >> b)
    {
        v.push_back(b);
    }

}


int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t;cin>>t;
    while(t > 0)
    {
        ans();
        t--;
    }
    return 0;
}